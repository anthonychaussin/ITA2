{% extends 'base.html.twig' %}

{% block title %}LikeP{% endblock %}

{% block body %}
    <h1>LikeP</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ like_p.id }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('like_p_index') }}">back to list</a>

    <a href="{{ path('like_p_edit', {'id': like_p.id}) }}">edit</a>

    {{ include('like_p/_delete_form.html.twig') }}
{% endblock %}
