{% extends 'base.html.twig' %}

{% block title %}DisApprouve{% endblock %}

{% block body %}
    <h1>DisApprouve</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ dis_approuve.id }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('dis_approuve_index') }}">back to list</a>

    <a href="{{ path('dis_approuve_edit', {'id': dis_approuve.id}) }}">edit</a>

    {{ include('dis_approuve/_delete_form.html.twig') }}
{% endblock %}
